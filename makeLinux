all:
	nvcc -c src/lib.cu
	nvcc -c test/tests.cu
	g++ -c src/main.cpp
	nvcc lib.o tests.o -o tests
	nvcc main.o lib.o -o exe
run_test:
	chmod +x exe
	./tests
clean:
	$(RM) exe tests lib.o main.o tests.o
