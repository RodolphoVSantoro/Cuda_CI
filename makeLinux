all:
	nvcc --device-emulation -c src/lib.cu
	nvcc --device-emulation -c test/tests.cu
	g++ -c src/main.cpp
	nvcc --device-emulation lib.o tests.o -o tests
	nvcc --device-emulation main.o lib.o -o exe
run_test:
	chmod +x exe
	./tests
clean:
	$(RM) exe tests lib.o main.o tests.o
